\documentclass[a4paper]{article}
\newcommand{\codeip}[1]{\texttt{#1}}
\usepackage{listings}
\usepackage{float}
\usepackage{placeins}
\usepackage{tcolorbox}
\usepackage{vntex}
\usepackage{pdflscape}
\usepackage{rotating}
\usepackage{a4wide,amssymb,epsfig,latexsym,multicol,array,hhline,fancyhdr}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{float}
\usepackage{lastpage} 
\usepackage{enumerate}
\usepackage{color}
\usepackage{graphicx}				
\usepackage{array}
\usepackage{tabularx, caption}
\usepackage{multirow}
\usepackage[framemethod=tikz]{mdframed}
\usepackage{multicol}
\usepackage{rotating}
\usepackage{graphics}
\usepackage{wrapfig}
\usepackage{geometry}
\usepackage{setspace}
\usepackage{epsfig}
\usepackage{tikz}
\usepackage{listings}
\usetikzlibrary{arrows,snakes,backgrounds}
\usepackage{hyperref}
\usepackage{minted}
\usepackage{indentfirst}
\usepackage{cases} 
\usepackage{listings}
\usepackage{algorithm}
\usepackage{algpseudocode}
\renewcommand{\baselinestretch}{1.5}
\usepackage{xcolor}
\renewcommand{\figurename}{Hình}

\newtcolorbox{softbox}{
    colback=black!3,        
    colframe=black!3,       
    arc=8pt,                
    boxrule=0pt,           
    left=10pt, right=10pt, top=8pt, bottom=8pt, 
    fontupper=\itshape\color{black!80} 
}

\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}

\definecolor{termBG}{HTML}{0C1C23} 
\definecolor{termRed}{HTML}{FF5555}   
\definecolor{termYellow}{HTML}{F1FA8C}
\definecolor{termWhite}{HTML}{F8F8F2} 
\definecolor{termGray}{HTML}{A9B7C6} 

\definecolor{termLightBG}{HTML}{F5F7FA} 
\definecolor{termLightBorder}{HTML}{E2E8F0}
\definecolor{termLightText}{HTML}{334155} 

\lstdefinestyle{terminalstyle}{
    basicstyle=\ttfamily\footnotesize\color{termLightText},
    backgroundcolor=\color{termLightBG},
    breaklines=true,
    columns=fullflexible,
    keepspaces=true,
    showstringspaces=false,
    frame=single,
    rulecolor=\color{termLightBorder},
    framesep=6pt,
    numbers=none,
    xleftmargin=2pt,
    xrightmargin=2pt
}

\lstdefinestyle{pythonstyle}{
    backgroundcolor=\color{backcolour},   
    commentstyle=\color{codegreen},
    keywordstyle=\color{blue},
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\ttfamily\footnotesize\color{black},
    breakatwhitespace=false,         
    breaklines=true,                 
    captionpos=b,                    
    keepspaces=true,                 
    showspaces=false,                
    showstringspaces=false,
    showtabs=false,                  
    tabsize=4,
    frame=single,
    rulecolor=\color{black!30}
}

\newcommand{\cred}[1]{\textcolor{termRed}{#1}}
\newcommand{\cyellow}[1]{\textcolor{termYellow}{#1}}

\usepackage{longtable}
\usepackage[shortlabels]{enumitem}
\hypersetup{urlcolor=blue,linkcolor=black,citecolor=black,colorlinks=true} 

\setlength{\headheight}{40pt}
\pagestyle{fancy}
\fancyhead{} % clear all header fields
\fancyhead[L]{
 \begin{tabular}{rl}
    \begin{picture}(25,15)(0,0)
    \put(0,-8){\includegraphics[width=8mm, height=8mm]{image/khtn.png}}
    \end{picture}&
	\begin{tabular}{l}
		\textbf{\bf \ttfamily Trường đại học Khoa Học Tự Nhiên TP.HCM}\\
		\textbf{\bf \ttfamily Khoa Công Nghệ Thông Tin}
	\end{tabular}  	
 \end{tabular}
}
\fancyhead[R]{
	\begin{tabular}{l}
		\tiny \bf \\
		\tiny \bf 
	\end{tabular}  }
\fancyfoot{} 
\fancyfoot[L]{\scriptsize \ttfamily Toán ứng dụng và thống kê}
\fancyfoot[R]{\scriptsize \ttfamily Trang {\thepage}/\pageref{page:lastpage}}
\renewcommand{\headrulewidth}{0.3pt}
\renewcommand{\footrulewidth}{0.3pt}

\setcounter{secnumdepth}{4}
\setcounter{tocdepth}{3}
\makeatletter
\newcounter {subsubsubsection}[subsubsection]
\renewcommand\thesubsubsubsection{\thesubsubsection .\@alph\c@subsubsubsection}
\newcommand\subsubsubsection{\@startsection{subsubsubsection}{4}{\z@}%
                                     {-3.25ex\@plus -1ex \@minus -.2ex}%
                                     {1.5ex \@plus .2ex}%
                                     {\normalfont\normalsize\bfseries}}
\newcommand*\l@subsubsubsection{\@dottedtocline{3}{10.0em}{4.1em}}
\newcommand*{\subsubsubsectionmark}[1]{}
\makeatother

\definecolor{dkgreen}{rgb}{0,0.6,0}
\definecolor{gray}{rgb}{0.5,0.5,0.5}
\definecolor{mauve}{rgb}{0.58,0,0.82}

\lstset{frame=tb,
	language=Matlab,
	aboveskip=3mm,
	belowskip=3mm,
	showstringspaces=false,
	columns=flexible,
	basicstyle={\small\ttfamily},
	numbers=none,
	numberstyle=\tiny\color{gray},
	keywordstyle=\color{blue},
	commentstyle=\color{dkgreen},
	stringstyle=\color{mauve},
	breaklines=true,
	breakatwhitespace=true,
	tabsize=3,
	numbers=left,
	stepnumber=1,
	numbersep=1pt,    
	firstnumber=1,
	numberfirstline=true
}

\begin{document}

\newtcbox{\code}{on line, 
  arc=3pt,            
  colback=gray!20,    
  colframe=gray!20,   
  boxrule=0pt,        
  boxsep=0pt,         
  left=2pt, right=2pt, top=2pt, bottom=2pt, 
  fontupper=\ttfamily 
}


\begin{titlepage}
\begin{center}
ĐẠI HỌC QUỐC GIA THÀNH PHỐ HỒ CHÍ MINH\\
TRƯỜNG ĐẠI HỌC KHOA HỌC TỰ NHIÊN\\
KHOA CÔNG NGHỆ THÔNG TIN
\end{center}

\vspace{0.7cm}

\begin{figure}[h!]
\begin{center}
\includegraphics[width=5cm]{image/khtn.png}
\end{center}
\end{figure}

\vspace{0.5cm}


\begin{center}
\begin{tabular}{c}
\multicolumn{1}{c}{\textbf{{\normalsize TOÁN ỨNG DỤNG VÀ THỐNG KÊ}}}\\
~~\\
\hline
\\
\multicolumn{1}{c}{\textbf{{\Large ĐỒ ÁN 1}}}\\
\textbf{{\Large}Ma Trận và Cơ Sở của Tính Toán Khoa Học}\\
\\
\hline
\end{tabular}
\end{center}

\vspace{0.5cm}

\noindent \hspace{3.1cm} % Giữ khoảng cách lề trái như cũ
\begin{tabular}{l l l}
    \textbf{Giảng viên hướng dẫn:} & ThS. Võ Nam Thục Đoan & \\
    & ThS. Lê Nhựt Nam \\
    \\
    \textbf{Nhóm 02:} & Phạm Minh Trọng &  24120151 \\
                                      & Đào Tiến Đạt    &  24120033 \\
                                      & Bùi Nhật Bảo    &  24120167 \\
                                      & Trịnh Kim Mai   &  24120199 \\
                                      & Trần Công Quang &  24120221 \\
\end{tabular}
\vspace{2.0cm}
\begin{center} {\footnotesize Thành phố Hồ Chí Minh, 4/2026}
\end{center}
\end{titlepage}
\thispagestyle{empty}
\newpage

% ==================== MỤC LỤC ====================
\tableofcontents
\newpage

% ==================== LỜI NÓI ĐẦU ====================
\addcontentsline{toc}{section}{Lời Nói Đầu}
Trong bối cảnh bùng nổ của trí tuệ nhân tạo và khoa học dữ liệu, nền tảng đại số tuyến tính và tính toán Khoa học đóng vai trò đặc biệt quan trọng, là điều kiện trong thể thiếu trong mọi hệ thống học máy, nén dữ liệu và tối ưu hóa hiện đại. Đồ án \textbf{"Ma Trận và Cơ Sở của Tính Toán Khoa Học"} không chỉ đơn thuần là một bài tập lớn, mà còn là cơ hội để chúng em tiếp cận được những kiến thức mới hơn, biết được ứng dụng của đại số tuyến tính vào trong cuộc sống, xây dựng được nền tảng để bước vào chuyên ngành

Dù đã nỗ lực hết sức và nghiêm túc trong công việc hoàn thiện đồ án , song do những giới hạn về mặt thời gian cũng như kinh nghiệm thực tiễn, báo cáo này chắc chắn vẫn còn những thiếu sót nhất định. Chúng em rất mong nhận được sự đánh giá, góp ý quý báu từ quý Thầy Cô để nhóm có thể đúc kết thêm kinh nghiệm và hoàn thiện bản thân hơn trong những chặng đường học tập tiếp theo.

\textbf{Chúng em xin chân thành cảm ơn!}
\newpage

% ==================== LỜI CAM ĐOAN ====================
\addcontentsline{toc}{section}{Lời Cam Đoan}
Chúng em, Nhóm 02, xin cam đoan đây là đồ án tự nghiên cứu và cài đặt của cả nhóm. Các dòng mã nguồn, phân tích thuật toán, cấu trúc dữ liệu và logic xử lý đều được nhóm tự cài đặt từ đầu bằng Python theo đúng yêu cầu của đồ án, hoàn toàn không sử dụng các hàm có sẵn của các thư viện như NumPy/SciPy để gian lận

Mọi kết quả kiểm thử, số liệu thực nghiệm, video trực quan hóa bằng Manim được trình bày trong báo cáo này là hoàn toàn trung thực, phản ánh đúng kết quả thực thi từ mã nguồn của nhóm. Nếu có bất kỳ sự vi phạm đạo đức học thuật nào, chúng em xin hoàn toàn chịu trách nhiệm trước giảng viên hướng dẫn và Khoa Công nghệ Thông tin, Trường Đại học Khoa học Tự nhiên, ĐHQG-HCM.

\vspace{1cm}
\begin{flushright}
\textbf{Đại diện nhóm} \\
\vspace{1.5cm}
\textit{Phạm Minh Trọng}
\end{flushright}
\newpage

% ==================== BẢNG PHÂN CÔNG CÔNG VIỆC ====================
\addcontentsline{toc}{section}{Bảng Phân Công Công Việc}
\begin{table}[H]
    \centering
    \renewcommand{\arraystretch}{1.4} % Tăng khoảng cách dòng
    \begin{tabular}{|c|p{4cm}|p{6.5cm}|c|}
        \hline
        \textbf{STT} & \textbf{Họ và Tên} & \textbf{Nhiệm vụ phân công} & \textbf{Hoàn thành} \\ \hline
        1 & Phạm Minh Trọng \newline \textbf{(24120151 - Leader)} & Phân chia nhiệm vụ, quản lý tiến độ làm việc, dựng kịch bản, trực quan hóa bằng Manim (part2), lồng giọng vào video, hoàn thiện báo cáo & 100\% \\ \hline
        2 & Đào Tiến Đạt \newline \textbf{(24120033)} & Cài đặt hàm giải hệ phương trình (\code{gaussian.py}), nghịch đảo ma trận (\code{inverse.py}) và phân rã LU (part3). Xây dựng \code{.ipynb} part1 & 100\% \\ \hline
        3 & Bùi Nhật Bảo \newline \textbf{(24120167)} & Dựng kịch bản, trực quan hóa bằng Manim (part2), viết script cho video, hoàn thiện báo cáo & 100\% \\ \hline
        4 & Trịnh Kim Mai \newline \textbf{(24120199)} & Xây dựng hàm tính định thức (\code{determinant.py}) và hạng, cơ sở (rank\_basis), tạo hàm xây dựng ma trận SPD và Hilbert (part 3). Chỉnh sửa và kiểm thử \code{.ipynb} part 1. & 100\% \\ \hline
        5 & Trần Công Quang \newline \textbf{(24120221)} & Cài đặt chéo hóa và phân rã với kỹ thuật SVD, xây dựng hàm gauss seidel và phân tích thời gian chương trình thực thi. Hoàn thiện phần phân tích (\code{.ipynb}) part 3  & 100\% \\ \hline
    \end{tabular}
    \caption{Chi tiết phân công nhiệm vụ của các thành viên trong nhóm}
\end{table}
\newpage

% ==================== PHẦN 1 ====================
\section{Phần 1: Phép khử Gauss và Các Ứng Dụng}

\subsection{Cơ sở lý thuyết}
\subsubsection{Hệ phương trình tuyến tính và Ma trận tăng cường}
Xét hệ phương trình tuyến tính có dạng tổng quát:
\begin{equation}
    Ax = b
\end{equation}
Trong đó:
\begin{itemize}
    \item $A \in \mathbb{R}^{m \times n}$: Ma trận hệ số.
    \item $x \in \mathbb{R}^n$: Vector ẩn số.
    \item $b \in \mathbb{R}^m$: Vector vế phải.
\end{itemize}
Để giải hệ phương trình, ta thiết lập Ma trận tăng cường $[A|b]$ bằng cách ghép vector $b$ vào bên phải ma trận $A$.

\subsubsection{Các phép biến đổi dòng sơ cấp}
Thuật toán dựa trên ba phép biến đổi cơ bản không làm thay đổi tập nghiệm của hệ phương trình:
\begin{enumerate}
    \item Hoán đổi dòng: $R_i \leftrightarrow R_j$
    \item Nhân dòng với một hằng số khác 0: $R_i \leftarrow c \cdot R_i \quad (c \neq 0)$
    \item Cộng vào một dòng bội số của dòng khác: $R_i \leftarrow R_i + c \cdot R_j$
\end{enumerate}

\subsubsection{Dạng bậc thang (REF) và Bậc thang rút gọn (RREF)}
\begin{itemize}
    \item \textbf{Dạng bậc thang (Row Echelon Form - REF):} Các dòng bằng 0 nằm dưới cùng; phần tử khác 0 đầu tiên của một dòng (phần tử chốt - pivot) luôn nằm bên phải phần tử chốt của dòng phía trên nó.
    \item \textbf{Dạng bậc thang rút gọn (Reduced Row Echelon Form - RREF):} Là dạng REF nhưng mọi phần tử chốt đều bằng $1$ và là phần tử khác 0 duy nhất trong cột chứa nó.
\end{itemize}

\subsubsection{Khử Gauss có chọn phần tử chốt (Partial Pivoting)}
Để tăng độ ổn định số học và tránh lỗi chia cho $0$, ta chọn phần tử chốt có giá trị tuyệt đối lớn nhất trong cột hiện tại trước khi thực hiện phép khử:
\begin{equation}
    |a_{pk}| = \max_{i \geq k} |a_{ik}|
\end{equation}

\subsubsection{Tính định thức qua khử Gauss}
Sau khi đưa ma trận $A$ về dạng tam giác trên $U$ bằng phép khử Gauss với $s$ lần hoán đổi dòng, định thức được tính theo công thức:
\begin{equation}
    \det(A) = (-1)^s \cdot \prod_{i=1}^{n} u_{ii}
\end{equation}

\subsubsection{Ma trận nghịch đảo bằng Gauss-Jordan}
\textbf{Định lý:} Ma trận $A \in \mathbb{R}^{n \times n}$ khả nghịch khi và chỉ khi $\det(A) \neq 0$.

Ma trận nghịch đảo $A^{-1}$ được tìm bằng cách thực hiện các phép biến đổi dòng sơ cấp đồng thời trên ma trận ghép $[A | I_n]$ cho đến khi ma trận bên trái đạt dạng đơn vị:
\begin{equation}
    [A | I_n] \xrightarrow{\text{biến đổi dòng}} [I_n | A^{-1}]
\end{equation}

\subsubsection{Hạng ma trận và cơ sở}
\textbf{Định nghĩa:} Hạng của ma trận $A$, ký hiệu là $\text{rank}(A)$, bằng số dòng khác 0 trong dạng REF của $A$, hoặc tương đương, bằng số cột pivot. Từ dạng RREF, ta xác định được ba không gian quan trọng:
\begin{itemize}
    \item \textbf{Không gian cột $\mathcal{C}(A)$:} Sinh bởi các cột pivot của $A$ gốc.
    \item \textbf{Không gian dòng $\mathcal{R}(A)$:} Sinh bởi các dòng khác 0 trong RREF.
    \item \textbf{Không gian nghiệm $\mathcal{N}(A)$:} Tập nghiệm của hệ thuần nhất $Ax = 0$.
\end{itemize}

\subsection{Thuật toán khử Gauss (Partial Pivoting)}
Quy trình thực hiện được nhóm cài đặt gồm hai giai đoạn chính:
\begin{itemize}
    \item \textbf{Giai đoạn 1: Khử xuôi (Forward Elimination)}
    \begin{enumerate}
        \item Khởi tạo ma trận tăng cường $M = [A|b]$.
        \item Với mỗi cột $k$: 
        \item Tìm dòng $p$ chứa phần tử có trị tuyệt đối lớn nhất kể từ dòng $k$ trở xuống.
        \item Hoán đổi dòng $k$ và dòng $p$.
        \item Với mỗi dòng $i > k$: Thực hiện phép biến đổi dòng để triệt tiêu các phần tử bên dưới phần tử chốt $M_{kk}$.
    \end{enumerate}
    
    \item \textbf{Giai đoạn 2: Thế ngược (Back Substitution)}\\
    Tìm nghiệm $x_i$ theo công thức truy hồi từ dòng cuối cùng lên:
    \begin{equation}
        x_i = \frac{M_{i, n+1} - \sum_{j=i+1}^{n} M_{ij} \cdot x_j}{M_{ii}}
    \end{equation}
\end{itemize}

\subsection{Triển khai và cài đặt chi tiết (Phân tích mã nguồn)}
Trong phần này, nhóm tiến hành xây dựng các thuật toán từ đầu sử dụng Python cơ bản. Để đảm bảo triệt tiêu sai số làm tròn (round-off error), mọi phép tính phân số được bảo toàn nhờ đối tượng \code{fractions.Fraction}. Gồm các hàm cụ thể như sau:

\subsubsection{Hàm \code{gaussian\_elimination(A, b)}}
Hàm đóng vai trò nền tảng, tiến hành biến đổi ma trận $A$ và $b$ về dạng tương đương bậc thang (REF).
\begin{itemize}
    \item \textbf{Cơ chế hoạt động:} Ở mỗi cột, thuật toán tìm phần tử có trị tuyệt đối lớn nhất từ dưới lên (Partial Pivoting) rồi đưa lên vị trí chốt thông qua hoán vị dòng, bảo vệ hệ thống khỏi lỗi chia số $0$.
    \item \textbf{Cơ chế tích hợp giải nghiệm sâu:} Thuật toán tối ưu hóa mặt luồng chéo dòng bằng việc tự động trích xuất $U$, $c\_result$ để đẩy qua hàm \code{back\_substitution} nội bộ, giúp xuất khẩu ra ngoài một bộ `M, x, s` khép kín một cách hoàn hảo.
\end{itemize}
\begin{lstlisting}[language=Python, style=pythonstyle]
# Ky thuat Partial Pivoting: Tim phan tu chot lon nhat tren cot hien tai
p = r
for i in range(r + 1, n):
    if abs(M[i][c_idx]) > abs(M[p][c_idx]):
        p = i

if p != r:
    M[r], M[p] = M[p], M[r]  # Hoan vi hang de giu su on dinh
    s += 1                   # Ghi nhan so lan hoan vi cho dinh thuc
\end{lstlisting}

\subsubsection{Hàm \code{back\_substitution(U, c)}}
Tiến hành truy hồi nghiệm từ chốt cuối cùng lên trên sau khi khử về dạng bậc thang.
\begin{itemize}
    \item \textbf{Tự động quét hạng động (rank):} Thuật toán tự động quét tìm dòng chứa giá trị $\neq 0$ để định lượng chính xác rank, cho phép ứng phó với bất kỳ định dạng bậc thang nào bị khuyết phần tử.
    \item \textbf{Cấu trúc tham số linh hoạt:} Với tình huống vô số nghiệm, nhóm sử dụng \code{Dictionary} để biểu diễn các hệ số tham số tổng quát dưới dạng ánh xạ.
\end{itemize}
\begin{lstlisting}[language=Python, style=pythonstyle]
# Bieu dien cac he so tham so tong quat duoi dang anh xa Dictionary
# VD: x[j] = {'const': gia_tri, index_bien_tu_do: he_so, ...}
x_expr = [{} for _ in range(n_cols)]

for j in free_cols:
    x_expr[j] = {'const': Fraction(0), j: Fraction(1)} # Kich hoat bien tu do
\end{lstlisting}

\subsubsection{Hàm \code{inverse(A)}}
Tìm nghịch đảo ma trận bằng cách biến đổi đồng thời trên vùng không gian mở rộng.
\begin{itemize}
    \item \textbf{Cơ chế hoạt động:} Bắt đầu từ trục tọa độ $[A | I_n]$, thuật toán chuẩn hóa đồng độ cho pivot về $1$, sau đó tiến hành quét sạch (trừ khử) toàn bộ các phần tử ở DƯỚI lẫn TRÊN trục pivot, cưỡng ép phần $A$ về ma trận $I$, đẩy $A^{-1}$ sang phải.
    \item \textbf{Bắt lỗi suy biến:} Ngay tại vòng lặp, nếu bắt gặp toàn cột bằng $0$ hoặc $\det(A) = 0$, hệ thống rẽ nhánh \code{None} tức thì để ngăn chặn hiện tượng crash hệ thống.
\end{itemize}
\begin{lstlisting}[language=Python, style=pythonstyle]
# Khu ca cac dong tren lan duoi de dua ma tran truc trai ve don vi I
for i in range(n):
    if i == k: continue   
    factor = M[i][k]
    for j in range(2 * n):
        M[i][j] -= factor * M[k][j]
    M[i][k] = Fraction(0)  # Tru gian tiep luon dat sai so ve 0 tuyet doi
\end{lstlisting}

\subsubsection{Hàm \code{determinant(A)}}
Hàm này áp dụng kết quả của phép khử để tính toán $\det$ theo tốc độ đa thức $\mathcal{O}(n^3)$.
\begin{itemize}
    \item \textbf{Tối ưu không gian:} Thu được lập tức nhờ tích đường chéo chính của khối bậc thang ở trên, nhân với dấu điều hướng $(-1)^s$ từ quá trình Partial Pivoting.
\end{itemize}
\begin{lstlisting}[language=Python, style=pythonstyle]
# M[i][i] la cac chot cua ma tran bac thang, tich cua chung -> det(U)
det = Fraction((-1) ** s)  # Bat dau voi dau dieu huong
for i in range(n):
    det *= M[i][i]
\end{lstlisting}

\subsubsection{Hàm \code{rank\_and\_basis(A)}}
Dò các điểm nối nền tảng của không gian vector tạo nên ma trận ban đầu.
\begin{itemize}
    \item \textbf{Cơ sở dòng và cột:} Trích cơ sở dòng từ biến cơ sở của $U$. Tuy vậy, thuật toán phải tra ngược thông qua index list để trích xuất dữ liệu gốc của cơ sở cột ngay trên trục của ma trận gốc.
    \item \textbf{Cơ sở nghiệm:} Cài đặt tuần tự các điểm neo (biến tự do). Giữ một điểm bằng $1$, các điểm khác về $0$, giải nghiệm hệ tuyến tính sinh ra các vector trục trực giao chính xác cho toàn bộ không gian nghiệm.
\end{itemize}
\begin{lstlisting}[language=Python, style=pythonstyle]
# Giai thuat sinh Co so Khong gian nghiem (Null Space Basis, Ax = 0)
for free_j in free_cols:
    special_solution = [Fraction(0)] * n_cols
    special_solution[free_j] = Fraction(1)  # Kich hoat diem Neo thu j

    for i, k in reversed(pivot_indices):
        sum_ax = sum(U[i][j] * special_solution[j] for j in range(k + 1, n_cols))
        special_solution[k] = -sum_ax / U[i][k]
    
    null_basis.append([str(val) for val in special_solution])
\end{lstlisting}

\subsubsection{Hàm \code{verify\_solution(A, x, b)}}
Quá trình kiểm tra được chia làm hai kịch bản rõ ràng và được thẩm định bằng \code{numpy.allclose(atol=1e-9)}:
\begin{itemize}
    \item \textbf{Trường hợp nghiệm duy nhất:} Chỉ cần ép vòng mảng phân số sang \code{float} và nhân ngược lại $A \cdot x$. Nếu kết quả xấp xỉ bằng $b$, chứng tỏ thuật toán tính đúng.
    \item \textbf{Trường hợp vô số nghiệm:} Vì nghiệm trả về dưới dạng \code{Dictionary} khá phức tạp, nhóm đã phân tách nghiệm tổng quát ra làm hai phần để tự đối chiếu: 
    \begin{enumerate}
        \item \textit{Nghiệm riêng (phần hằng số $x_p$):} Kiểm tra xem $A \cdot x_p = b$ có khớp không.
        \item \textit{Nghiệm thuần nhất (các vector chứa neo biến tự do $v_k$):} Đối với mỗi tham số $t_k$, nhóm tiến hành bóc tách vector hệ số tương ứng và nhân ngược với $A$. Nếu $A \cdot v_k = \mathbf{0}$, nghĩa là vector đó thực sự khớp với không gian nghiệm.
    \end{enumerate}
\end{itemize}
\begin{lstlisting}[language=Python, style=pythonstyle]
# Doan code ma nhom dung de kiem tra chat che loi giai vo so nghiem:
# 1. Kiem tra phan hang so lam nhiem vu nghiem rieng: A * x_p = b
x_p = np.array([float(x[i].get('const', Fraction(0))) for i in range(n_cols)])
if not np.allclose(np.dot(A_np, x_p), b_np, atol=1e-9):
    print("-> SAI: Phan hang so khong khop!")
    return

# 2. Kiem tra phan vector tuong ung voi tung bien tu do: A * v_k = 0
for k in free_vars:
    v_k = np.array([float(x[i].get(k, Fraction(0))) for i in range(n_cols)])
    if not np.allclose(np.dot(A_np, v_k), np.zeros(len(b)), atol=1e-9):
        print(f"-> SAI: Vector he so cua bien tu do o t{k+1} khong thuoc Null Space!")
        return
\end{lstlisting}

\subsection{Đánh giá tiêu chí hoàn thành}

Dựa trên bảng tiêu chí đánh giá đồ án, nhóm đã rà soát và tự đánh giá mức độ hoàn thành các yêu cầu của Phần 1 như sau:

\begin{table}[H]
    \centering
    \renewcommand{\arraystretch}{1.4}
    \begin{tabular}{|p{4cm}|p{6.5cm}|c|}
        \hline
        \textbf{Tiêu chí} & \textbf{Mô tả yêu cầu} & \textbf{Mức độ hoàn thành} \\ \hline
        \textbf{Cài đặt Gauss cơ bản} & Chạy đúng, có kiểm thử rõ ràng & 100\% \\ \hline
        \textbf{Partial Pivoting} & Xử lý đúng khi pivot nhỏ hoặc bằng 0 & 100\% \\ \hline
        \textbf{Tính định thức} & Đúng dấu và giá trị & 100\% \\ \hline
        \textbf{Ma trận nghịch đảo} & Gauss-Jordan, kiểm thử $AA^{-1}=I$ & 100\% \\ \hline
        \textbf{Hạng và cơ sở} & Đúng rank, trả về cơ sở các không gian & 100\% \\ \hline
        \textbf{Kiểm chứng} & So sánh kết quả với NumPy/SciPy & 100\% \\ \hline
    \end{tabular}
    \caption{Đánh giá tiêu chí hoàn thành - Phần 1}
    \label{tab:criteria_part1}
\end{table}


% ==================== PHẦN 2 ====================
\newpage
\section{Phần 2: Phân Rã Ma Trận và Trực Quan Hóa với Manim}

\subsection{Chéo hóa ma trận (Matrix Diagonalization)}
\subsubsection{Cơ sở lý thuyết}
\textbf{Định nghĩa:} Một ma trận vuông $A$ cấp $n \times n$ được gọi là chéo hóa được (diagonalizable) nếu tồn tại một ma trận khả nghịch $P$ và một ma trận đường chéo $D$ sao cho:  
\begin{equation}
    A = P \cdot D \cdot P^{-1}
\end{equation}
Trong đó:
\begin{itemize}
    \item $D = \text{diag}(\lambda_1, \lambda_2, \dots, \lambda_n)$ là ma trận chứa các \textit{giá trị riêng} (eigenvalues) của $A$ trên đường chéo chính.
    \item $P = [v_1 | v_2 | \dots | v_n]$ là ma trận mà các cột là các \textit{vector riêng} (eigenvectors) tương ứng với các giá trị riêng đó.
\end{itemize}

\textbf{Điều kiện cần và đủ:} Ma trận $A$ cấp $n$ chéo hóa được khi và chỉ khi nó có $n$ vector riêng độc lập tuyến tính. Điều này tương đương với việc tổng số chiều của các không gian riêng (Geometric Multiplicity) phải bằng $n$. Nếu ma trận có $n$ giá trị riêng phân biệt thì chắc chắn chéo hóa được.

\subsubsection{Thuật toán cài đặt}
Nhóm triển khai quy trình chéo hóa (trong \code{diagonalization.py}), ma trận \(n \times n\) với \(n \le 5\) thì tự cài đặt thủ công bằng cách \textbf{Lặp QR} để rút trị riêng. Nhưng với ma trận \(n > 5\) thì nhóm sử dụng hàm \code{eigvals} của thư viện \code{numpy.linalg} để rút trị riêng. Cuối cùng sẽ khử Gauss (kế thừa từ phần 1) để xây vector riêng.

\textbf{A. Xử lý triệt để Ma trận khuyết (Defective Matrix)}\\
Điểm sáng thuật toán của nhóm là hàm \code{CheckDefective\_Eigvals}. Trước khi dựng phương trình vector riêng, thuật toán sẽ đối chiếu Bội hình học (Geometric Multiplicity - GM) và Bội đại số (Algebraic Multiplicity - AM). Nếu $GM < AM$, ma trận lập tức bị từ chối chéo hóa nhằm tránh ma trận $P$ bị suy biến.
\begin{lstlisting}[language=Python, style=pythonstyle]
# Doan code chan dung loi toan hoc the hien tu duy Chat che
AM = rounded_eigs.count(lamda) # Tinh Boi dai so
if AM > 1:
    A_minus_lamdaI = subtract_lambda_I(A, lamda)
    res_temp = rank_and_basis(A_minus_lamdaI)
    rank = res_temp['rank']
    GM = n - rank  # Tinh Boi hinh hoc tu Hang (Rank-Nullity)
    
    if GM < AM:    # Phat hien ma tran khuyet, thieu he vector!
        return True
\end{lstlisting}

\textbf{B. Phân rã RQ $\rightarrow$ QR hội tụ trị riêng}\\
Áp dụng phép lặp trực giao Gram-Schmidt biến $A_k = Q \cdot R$, nhân ngược $A_{k+1} = R \cdot Q$. Cấu trúc này dồn phần tử ngoài đường chéo về 0, chừa lại các trị riêng ròng ở đường chéo trung tâm. Các chiều không gian sau đó được quét bằng \code{rank\_and\_basis}.
\subsection{Phân rã Giá trị suy biến (SVD)}
\subsubsection{Cơ sở lý thuyết}
Phân rã SVD là phương pháp phân tích tổng quát áp dụng cho mọi ma trận $A \in \mathbb{R}^{m \times n}$. Ma trận $A$ được phân tích thành:
\begin{equation}
    A = U \cdot \Sigma \cdot V^T
\end{equation}
Trong đó:
\begin{itemize}
    \item $U \in \mathbb{R}^{m \times m}$: Ma trận trực giao chứa các vector suy biến trái (các vector riêng của $AA^T$).
    \item $V \in \mathbb{R}^{n \times n}$: Ma trận trực giao chứa các vector suy biến phải (các vector riêng của $A^TA$).
    \item $\Sigma \in \mathbb{R}^{m \times n}$: Ma trận đường chéo chứa các giá trị suy biến $\sigma_1 \geq \sigma_2 \dots \geq \sigma_p \geq 0$ với $p = \min(m, n)$.
\end{itemize}
Về mặt hình học, SVD biểu diễn biến đổi tuyến tính qua 3 bước: \textit{Xoay} ($V^T$) $\rightarrow$ \textit{Co giãn trục} ($\Sigma$) $\rightarrow$ \textit{Xoay đích} ($U$).

\subsubsection{Phân rã tĩnh bằng phép Xoay Jacobi (Jacobi Eigenvalues)}
Thay vì dùng QR, nhóm lựa chọn thuật toán \textbf{Jacobi} xử lý riêng biệt cho ma trận đối xứng liên kết $M = A^T A$ vì tính ổn định số học vững chắc, mã nguồn được nhóm xây dựng tại \code{decomposition.py}.

Hàm hoạt động bằng cách đi tìm các phần tử lệch đường chéo $M[p][q]$, sau đó tính góc xoay $\theta$ để đưa nó về 0:
\begin{lstlisting}[language=Python, style=pythonstyle]
# Doan co so luong giac de triet tieu bat ky diem roi lech nao tren hang/cot
if M[p][p] == M[q][q]:
    theta = mth.pi / 4.0
else:
    theta = 0.5 * mth.atan(2.0 * M[p][q] / (M[p][p] - M[q][q]))
cos_t, sin_t = mth.cos(theta), mth.sin(theta)

# Don vi nang tuong lay goc cua M_pq tro ve duong cheo chinh
M_pp = cos_t**2 * M[p][p] - 2*sin_t*cos_t*M[p][q] + sin_t**2 * M[q][q]
M[p][q] = M[q][p] = 0.0 # Clear hoan toan khoi ban do!
\end{lstlisting}
Sau vòng lặp đưa $M[p][q]$ về 0, $M$ lộ ra ma trận đường chéo gồm các $\lambda_i$. Thuật toán kết thúc khi $\Sigma$ (bằng $\sqrt{\lambda_i}$) và dựng trục suy biến trái $U$ ($u_i = \frac{A v_i}{\sigma_i}$) độc lập hoàn toàn với thư viện.

\subsection{Ứng dụng và Trực quan hóa với Manim}

\subsubsection{Ứng dụng trong Hệ thống gợi ý (Recommendation Systems)}

Hệ thống gợi ý là một ứng dụng tiêu biểu của phân rã SVD nhằm cá nhân hóa trải nghiệm người dùng trên các nền tảng số hiện đại. Thay vì chỉ nhìn vào các đánh giá trực tiếp, thuật toán này giúp khám phá những mối liên hệ ngầm giữa sở thích khách hàng và đặc tính sản phẩm thông qua việc trích xuất các yếu tố tiềm ẩn. Đây là công cụ mạnh mẽ để xử lý dữ liệu thưa thớt, giúp dự đoán nhu cầu và mang lại những đề xuất chính xác nhất cho người dùng.

\subsubsubsection{Ma trận Người dùng - Mục tiêu (User-Item Matrix)}
Trong các hệ thống thương mại điện tử hoặc dịch vụ xem phim trực tuyến, dữ liệu về sự tương tác của người dùng thường được biểu diễn dưới dạng một ma trận $A \in \mathbb{R}^{m \times n}$. Trong đó:
\begin{itemize}
    \item $m$ là số lượng người dùng (users).
    \item $n$ là số lượng sản phẩm hoặc bộ phim (items).
    \item Phần tử $a_{ij}$ đại diện cho mức độ yêu thích (rating) của người dùng $i$ đối với sản phẩm $j$.
\end{itemize}
Thách thức lớn nhất là ma trận này thường rất thưa (sparse) vì một người dùng chỉ có thể xem hoặc đánh giá một phần rất nhỏ trong hàng triệu sản phẩm hiện có. Mục tiêu của hệ thống gợi ý là dự đoán các giá trị còn thiếu trong ma trận để đưa ra gợi ý phù hợp.

\begin{figure}[h]
    \centering
    % Ghi chú: Chèn hình minh họa ma trận User-Item với nhiều ô trống (NaN/?)
    \includegraphics[width=0.8\textwidth]{image/rating.png}
    \caption{Ma trận User-Item với các giá trị đánh giá còn thiếu.}
\end{figure}

\subsubsubsection{Cơ chế lọc dựa trên SVD (SVD-based Collaborative Filtering)}
Bằng cách áp dụng phân rã $A \approx U_k \Sigma_k V_k^T$, SVD giúp chúng ta trích xuất các "yếu tố tiềm ẩn" (latent factors) quyết định hành vi người dùng:
\begin{itemize}
    \item Ma trận $U_k$ (kích thước $m \times k$): Đại diện cho sở thích của người dùng đối với các đặc tính ẩn.
    \item Ma trận $V_k^T$ (kích thước $k \times n$): Đại diện cho mức độ sở hữu các đặc tính ẩn của từng sản phẩm.
    \item Ma trận $\Sigma_k$: Trọng số của các yếu tố ẩn này.
\end{itemize}
Khi đó, giá trị dự đoán $\hat{a}_{ij}$ cho một ô trống bất kỳ được tính bằng tích vô hướng của vector đại diện người dùng và vector đặc trưng sản phẩm.

\subsubsubsection{Quy trình thực hiện gợi ý}
\begin{enumerate}
    \item \textbf{Khởi tạo:} Điền các giá trị mặc định (thường là trung bình đánh giá) vào các ô trống trong ma trận $A$.
    \item \textbf{Phân rã:} Thực hiện SVD trên ma trận đã điền để chọn ra $k$ thành phần quan trọng nhất.
    \item \textbf{Dự đoán:} Tính toán ma trận xấp xỉ $A_k = U_k \Sigma_k V_k^T$. Các giá trị tại các vị trí trước đó bằng 0/trống giờ đây sẽ có một con số dự đoán.
    \item \textbf{Gợi ý:} Sắp xếp các giá trị dự đoán này theo thứ tự giảm dần và chọn ra Top-N sản phẩm có điểm cao nhất để gửi đến người dùng.
\end{enumerate}

\subsubsubsection{Ưu điểm và hạn chế}
\begin{itemize}
    \item \textbf{Ưu điểm:} Giảm chiều dữ liệu mạnh mẽ, loại bỏ nhiễu và tìm ra được những mối liên hệ ngầm giữa các bộ phim mà các phương pháp so sánh trực tiếp không thấy được.
    \item \textbf{Hạn chế:} Vấn đề "Cold Start" (Người dùng mới hoặc sản phẩm mới chưa có dữ liệu đánh giá) và chi phí tính toán khi số lượng người dùng tăng lên hàng triệu người.
\end{itemize}

\begin{figure}[h]
    \centering
    % Ghi chú: Chèn biểu đồ hoặc hình minh họa quá trình nén từ User-Item Matrix sang Latent Factors
    \includegraphics[width=0.8\textwidth]{image/rating_1.png}
    \caption{Mô hình hóa sở thích người dùng thông qua các yếu tố tiềm ẩn bằng SVD.}
\end{figure}



\subsubsection{Ứng dụng trong nén ảnh kỹ thuật số}

Nén ảnh là một trong những ứng dụng trực quan và hiệu quả nhất của phân rã SVD trong lĩnh vực đại số tuyến tính số. Kỹ thuật này giúp chuyển đổi các ma trận dữ liệu hình ảnh phức tạp thành những thành phần rút gọn, từ đó loại bỏ thông tin dư thừa mà vẫn giữ được các đặc trưng cốt lõi nhất. Đây là giải pháp tối ưu để cân bằng giữa dung lượng lưu trữ và chất lượng hình ảnh trong các bài toán thực tế

\subsubsubsection{Biểu diễn ảnh dưới dạng ma trận}
Trong tính toán khoa học, một bức ảnh kỹ thuật số đa mức xám (grayscale) được xem như một ma trận $A \in \mathbb{R}^{m \times n}$, trong đó $m, n$ lần lượt là số pixel chiều dọc và chiều ngang. Mỗi phần tử $a_{ij}$ đại diện cho cường độ sáng của điểm ảnh. Đối với ảnh màu, dữ liệu được tách thành ba ma trận tương ứng với các kênh màu R (Red), G (Green) và B (Blue). Việc nén ảnh bằng SVD dựa trên việc xấp xỉ ma trận gốc bằng một ma trận có hạng (rank) thấp hơn để giảm không gian lưu trữ.

\begin{figure}[h]
    \centering
    % Ghi chú: Chèn ảnh minh họa ma trận hóa một bức ảnh (Pixel matrix)
    \includegraphics[width=1\textwidth]{image/image_1.PNG}
    \caption{Minh họa cấu trúc ma trận của ảnh kỹ thuật số.}
\end{figure}

\subsubsubsection{Cơ sở toán học với Xấp xỉ hạng thấp}
Phân rã SVD của ma trận $A$ được định nghĩa là $A = U\Sigma V^T$. Phép nhân này có thể được viết lại dưới dạng tổng của $p = \min(m, n)$ ma trận hạng 1:
\begin{equation}
A = \sigma_1 u_1 v_1^T + \sigma_2 u_2 v_2^T + \dots + \sigma_p u_p v_p^T = \sum_{i=1}^{p} \sigma_i u_i v_i^T
\end{equation}
Trong đó các giá trị suy biến được sắp xếp giảm dần: $\sigma_1 \ge \sigma_2 \ge \dots \ge \sigma_p \ge 0$. Ý tưởng cốt lõi của việc nén ảnh là chỉ giữ lại $k$ giá trị suy biến lớn nhất ($k \ll p$):
\begin{equation}
A_k = \sum_{i=1}^{k} \sigma_i u_i v_i^T
\end{equation}
Theo định lý Eckart-Young, $A_k$ là ma trận xấp xỉ tốt nhất cho $A$ trong số các ma trận có hạng $k$ (xét theo chuẩn Frobenius).

\subsubsubsection{Phân tích hiệu quả lưu trữ}
Để lưu trữ ma trận $A$, ta cần $m \times n$ đơn vị dữ liệu. Tuy nhiên, với xấp xỉ $A_k$, ta chỉ cần lưu trữ các thành phần:
\begin{itemize}
    \item $k$ vector trong $U$: $m \times k$ phần tử.
    \item $k$ giá trị suy biến trong $\Sigma$: $k$ phần tử.
    \item $k$ vector trong $V$: $n \times k$ phần tử.
\end{itemize}
Tổng số phần tử cần lưu trữ là $k(m + n + 1)$. Tỷ lệ nén được tính bằng:
\begin{equation}
\text{Compression Ratio} = \frac{m \times n}{k(m + n + 1)}
\end{equation}

Trong thực tế, máy tính thường lưu trữ mỗi điểm ảnh (pixel) dưới định dạng số nguyên không dấu 8-bit (\texttt{uint8}), tương ứng với mức dung lượng 1 byte cho mỗi phần tử của ma trận. Do đó, thay vì tiêu tốn $m \times n$ bytes cho toàn bộ ảnh gốc, việc lưu trữ dưới dạng nén chỉ đòi hỏi $k(m+n+1)$ bytes. Tỷ lệ dung lượng lưu trữ thực tế (Storage Ratio) được xác định như sau:\begin{equation}\text{Storage Ratio} = \frac{k(m+n+1) \text{ bytes}}{m \times n \text{ bytes}} = \frac{k(m+n+1)}{m \times n}\end{equation}
\subsubsubsection{Đánh giá thực nghiệm và Kết quả trực quan}
Việc chọn giá trị $k$ quyết định sự cân bằng giữa độ nét và dung lượng:
\begin{itemize}
    \item $k$ quá nhỏ: Ảnh bị mờ, chỉ giữ lại các khối sáng tối đại thể.
    \item $k$ vừa đủ: Ảnh tái tạo giữ được các đặc trưng quan trọng, mắt người khó phân biệt với ảnh gốc nhưng dung lượng giảm đáng kể.
\end{itemize}

\begin{figure}[htbp]
    \centering
    
    % Hàng 1
    \begin{subfigure}[b]{0.35\textwidth}
        \centering
        \includegraphics[width=\textwidth]{image/CMP_RGB_0.jpg}
        \caption{$k = 5$ (19.32 KB)} 
    \end{subfigure}
    \hspace{1cm} % Khoảng cách chiều ngang giữa 2 ảnh
    \begin{subfigure}[b]{0.35\textwidth}
        \centering
        \includegraphics[width=\textwidth]{image/CMP_RGB_2.jpg}
        \caption{$k = 20$ (38.64 KB)}
    \end{subfigure}
    
    \vspace{0.5cm} % Khoảng cách chiều dọc giữa 2 hàng

    % Hàng 2
    \begin{subfigure}[b]{0.35\textwidth}
        \centering
        \includegraphics[width=\textwidth]{image/CMP_RGB_3.jpg}
        \caption{$k = 50$ (77.28 KB)}
    \end{subfigure}
    \hspace{1cm}
    \begin{subfigure}[b]{0.35\textwidth}
        \centering
        \includegraphics[width=\textwidth]{image/messi22.jpg}
        
        \caption{$k = 583(max)$  (1255.38 KB)}
    \end{subfigure}

    \caption{Kết quả nén ảnh với các giá trị $k$ khác nhau.}
\end{figure}

\subsubsubsection{Nhận xét}
Phương pháp nén bằng SVD rất mạnh mẽ trong việc trích xuất đặc trưng và khử nhiễu. Tuy nhiên, trong thực tế, chi phí tính toán phân rã SVD cho ma trận lớn khá cao, do đó nó thường được ứng dụng trong phân tích dữ liệu ảnh hoặc nén các vùng ảnh đặc thù hơn là dùng làm chuẩn nén phổ thông như JPEG.

\subsubsection{Trực quan hóa với Manim về ứng dụng nén ảnh của SVD}
Để biến các khối công thức đại số tuyến tính trừu tượng trở nên trực quan và dễ tiếp cận, nhóm đã ứng dụng thư viện đồ họa toán học \textbf{Manim} để xây dựng công cụ đồ họa dạng video. Mã nguồn kịch bản phim được cấu trúc đối tượng hóa thành các phân cảnh (\code{Scene}) như sau:

\vspace{0.3cm}
\begin{mdframed}[linecolor=gray!50, linewidth=3pt, topline=false, bottomline=false, rightline=false, backgroundcolor=black!2, innerleftmargin=10pt, innertopmargin=8pt, innerbottommargin=8pt]
\textbf{\large \code{Section1\_IntroProblem}} \hfill \textit{Mở đầu bài toán}\\
\vspace{-0.2cm}\rule{\linewidth}{0.5pt}\vspace{0.2cm}\\
Đóng vai trò là phân cảnh mồi (hook/intro), dẫn dắt người xem vào không gian toán học. Khởi tạo một ma trận mẫu đứng trung tâm khung hình. Việc sử dụng các hiệu ứng làm nổi bật từng hệ số giúp định hướng sự tập trung một cách trơn tru.
\end{mdframed}

\vspace{0.2cm}
\begin{mdframed}[linecolor=gray!50, linewidth=3pt, topline=false, bottomline=false, rightline=false, backgroundcolor=black!2, innerleftmargin=10pt, innertopmargin=8pt, innerbottommargin=8pt]
\textbf{\large \code{MatrixDiagonalization}} \hfill \textit{Quá trình Chéo hóa}\\
\vspace{-0.2cm}\rule{\linewidth}{0.5pt}\vspace{0.2cm}\\
Tái hiện từng bước của thuật toán chéo hóa ma trận. Xâu chuỗi tính toán từ việc diễn giải đa thức đặc trưng để tìm \textit{giá trị riêng}, lập hệ phương trình để truy xuất \textit{vector riêng}. Quá trình gom ma trận chuyển cơ sở $P$ và ma trận đường chéo $D$ được trình bày song song qua các khối sáng màu, chứng minh đồ họa cho công thức $A = PDP^{-1}$.
\end{mdframed}

\vspace{0.2cm}
\begin{mdframed}[linecolor=gray!50, linewidth=3pt, topline=false, bottomline=false, rightline=false, backgroundcolor=black!2, innerleftmargin=10pt, innertopmargin=8pt, innerbottommargin=8pt]
\textbf{\large \code{Section23\_Theory\_And\_GeometricSVD}} \hfill \textit{Ý nghĩa hình học của SVD}\\
\vspace{-0.2cm}\rule{\linewidth}{0.5pt}\vspace{0.2cm}\\
Phân cảnh minh họa việc một đường tròn đơn vị và các vector bị biến dạng thành hình ellipse khi nhân trực tiếp với ma trận $A$. Sau đó video chia tách tiến trình thành $3$ nhịp SVD thuần túy: Góc xoay lưới Không gian ($V^T$), sự co giãn trục định hình ellipse ($\Sigma$), và điều hướng chỉnh góc lưới không gian khớp với bản sinh ($U$).
\end{mdframed}

\vspace{0.2cm}
\begin{mdframed}[linecolor=gray!50, linewidth=3pt, topline=false, bottomline=false, rightline=false, backgroundcolor=black!2, innerleftmargin=10pt, innertopmargin=8pt, innerbottommargin=8pt]
\textbf{\large \code{SVDAndDiagonalization}} \hfill \textit{Liên hệ SVD và chéo hóa}\\
\vspace{-0.2cm}\rule{\linewidth}{0.5pt}\vspace{0.2cm}\\
Cầu nối giao thoa bản chất giữa phân rã SVD và chéo hóa. Kịch bản lồng ghép các phương trình biểu diễn lên nhau. Ở trên cùng một phân cảnh, đồ họa hiển thị ma trận tích $A^TA$ vừa được khai triển qua SVD ($V\Sigma^2 V^T$) lại vừa bị chéo hóa cổ điển ($P \Lambda P^T$).
\end{mdframed}

\vspace{0.2cm}
\begin{mdframed}[linecolor=gray!50, linewidth=3pt, topline=false, bottomline=false, rightline=false, backgroundcolor=black!2, innerleftmargin=10pt, innertopmargin=8pt, innerbottommargin=8pt]
\textbf{\large \code{SVDDecompositionVisual} và \code{LowRankTheory}} \hfill \textit{Cơ chế xấp xỉ hạng thấp}\\
\vspace{-0.2cm}\rule{\linewidth}{0.5pt}\vspace{0.2cm}\\
Thực hiện lệnh bóc tách đa tầng phương trình cực trị $A = U\Sigma V^T$ thành một chuỗi tổng của các ma trận hạng 1 ($A_k$). Lớp \code{LowRankTheory} sau đó triển khai các khối đồ họa logic đóng khung bao bọc để chứng minh đẳng thức tỷ lệ lưu trữ bộ nhớ khi hệ thống chủ động cắt giảm $k$ hạng tử.
\end{mdframed}

\vspace{0.2cm}
\begin{mdframed}[linecolor=gray!50, linewidth=3pt, topline=false, bottomline=false, rightline=false, backgroundcolor=black!2, innerleftmargin=10pt, innertopmargin=8pt, innerbottommargin=8pt]
\textbf{\large \code{SVDImageCompression}} \hfill \textit{Ứng dụng nén ảnh SVD}\\
\vspace{-0.2cm}\rule{\linewidth}{0.5pt}\vspace{0.2cm}\\
Ứng dụng giải thuật vào đời sống để phân tách một bức ảnh kỹ thuật số hoàn chỉnh. Khi bậc xấp xỉ $k$ ngày càng tăng lên, các khối ảnh nhiễu được nạp tầng khôi phục dần sắc nét đường bao. Song song đó, thước đo (storage ratio) chạy thời gian thực trên màn hình.
\end{mdframed}

\subsubsection{Kết xuất và gộp video bằng FFmpeg}
Trong quá trình sử dụng Manim để render các \code{Scene}, mỗi đoạn mã phân cảnh sẽ được xuất ra thành một tệp video \code{.mp4} rời rạc. Để tạo ra một video truyền đạt nội dung xuyên suốt, nhóm đã ứng dụng phần mềm xử lý đa phương tiện \textbf{FFmpeg} nhằm nối các tệp video này lại với nhau mà không làm thay đổi chất lượng nội dung.

\textbf{1. Cài đặt FFmpeg:}\\
Trình xử lý đa phương tiện FFmpeg là nền tảng mã nguồn mở hoạt động dựa trên dòng lệnh (Command Line). Để hệ thống có thể gọi được lệnh \code{ffmpeg} ở bất kỳ thư mục nào, nhóm đã thực hiện cài đặt qua các bước sau:

\begin{itemize}
    \setlength\itemsep{0.3em}
    \item \textbf{Bước 1 (Tải mã nguồn):} Truy cập trang chủ \url{https://ffmpeg.org/} và tải xuống bản biên dịch sẵn (pre-built) dạng tập tin nén (.zip) dành cho Windows.
    \item \textbf{Bước 2 (Giải nén):} Mở tập tin ZIP và giải nén vào một vị trí cố định trên máy tính (ví dụ: \code{C:\textbackslash FFmpeg}).
    \item \textbf{Bước 3 (Thiết lập biến môi trường):} 
    \begin{itemize}
        \item Mở chức năng \textbf{Environment Variables} trên hệ điều hành Windows.
        \item Tại mục \textit{System variables}, tìm và chọn nút \textbf{Edit} trên biến \textbf{Path}.
        \item Nhấn \textbf{New} và thêm đường dẫn trỏ trực tiếp đến thư mục \code{bin/} của FFmpeg.
    \end{itemize}
    \item \textbf{Bước 4 (Kiểm tra hệ thống):} Mở Terminal (như Command Prompt hoặc PowerShell) và chạy lệnh \code{ffmpeg -version}. Nếu bảng thông tin phiên bản xuất hiện, quá trình liên kết đã thành công.
\end{itemize}

\textbf{2. Quá trình ghép video tuần tự (Concatenation):}\\
Tiến trình kết xuất các luồng video phân mảnh của Manim trải qua hai bước:
\begin{enumerate}[(a)]
    \item \textbf{Tạo tệp cấu hình danh sách video:} Nhóm khởi tạo một tệp văn bản \code{videos.txt} chứa danh sách các video thành phần muốn gộp với cú pháp \code{file 'tên\_video.mp4'}, được đặt tại chính thư mục lưu trữ các video và liệt kê theo trình tự xuất hiện của cấu trúc kịch bản phân cảnh:
\begin{lstlisting}[style=terminalstyle]
file 'Section1_IntroProblem.mp4'
file 'MatrixDiagonalization.mp4'
file 'Section23_Theory_And_GeometricSVD.mp4'
file 'SVDAndDiagonalization.mp4'
file 'SVDDecompositionVisual.mp4'
file 'SVDImageCompression.mp4'
\end{lstlisting}

    \item \textbf{Thực thi lệnh ghép nối nguyên bản không tái tạo (demuxer):}\\Thay vì mã hóa lại video (re-encode) gây mất mát chất lượng đồ họa pixel cũng như tiêu tốn rất nhiều thời gian phân giải bộ đệm, nhóm quyết định sử dụng chế độ truyền tải \code{concat demuxer} (copy trực tiếp luồng stream âm thanh và video của Manim) với các cờ sau:
\begin{lstlisting}[style=terminalstyle]
ffmpeg -f concat -safe 0 -i videos.txt -c copy final_video.mp4
\end{lstlisting}
    \begin{itemize}
        \item \code{-f concat}: Định dạng tệp nguồn đi vào là danh sách định tuyến phân mảnh text.
        \item \code{-safe 0}: Hủy kiểm soát cờ an toàn liên quan bảo mật đường dẫn tuyệt đối/tương đối.
        \item \code{-c copy}: Cờ đặc tả codec tiến hành Stream Copy 100\% lưu lượng pixel gốc sang siêu bộ đệm đầu ra \code{demo\_video.mp4} mà không cần sử dụng bộ tái định dạng (re-rendering).
    \end{itemize}
\end{enumerate}
Kết thúc tiến trình, nhóm đã xuất bản thành công dạng thức video cuối cùng với tốc độ khung hình và dải màu giống khuôn mẫu Manim một cách liền mạch.

\subsection{Đánh giá tiêu chí hoàn thành}

Dựa trên các yêu cầu cụ thể đối với phần phân rã ma trận và trực quan hóa, nhóm tự đánh giá kết quả thực hiện Phần 2 như sau:

\begin{table}[H]
    \centering
    \renewcommand{\arraystretch}{1.4}
    \begin{tabular}{|p{4cm}|p{6.5cm}|c|c|}
        \hline
        \textbf{Tiêu chí} & \textbf{Mô tả yêu cầu} & \textbf{Mức độ hoàn thành} \\ \hline
        \textbf{Lý thuyết phân rã} & Trình bày đầy đủ, chính xác công thức  & 100\% \\ \hline
        \textbf{Cài đặt Python} & Đúng, có kiểm chứng bằng NumPy  & 100\% \\ \hline
        \textbf{Chéo hóa ma trận} & Cài đặt giá trị riêng và chéo hóa  & 100\% \\ \hline
        \textbf{Video Manim} & Hoạt ảnh rõ ràng, đủ nội dung  & 100\% \\ \hline
    \end{tabular}
    \caption{Đánh giá tiêu chí hoàn thành - Phần 2}
    \label{tab:criteria_part2}
\end{table}

% ==================== PHẦN 3 ====================
\newpage
\section{Phần 3: Giải Hệ Phương Trình và Phân Tích Hiệu Năng}

\subsection{Mục tiêu thực nghiệm}
Nội dung này tập trung vào việc đánh giá, so sánh và đối chiếu các thuật toán giải hệ phương trình tuyến tính $Ax = b$ đã được nhóm tự cài đặt bằng Python. Các khía cạnh được khảo sát bao gồm:
\begin{itemize}
    \item \textbf{Độ phức tạp tính toán:} Kiểm chứng sự tương quan giữa thời gian thực thi thực tế và độ phức tạp lý thuyết $O(n^3)$ của các phương pháp trực tiếp.
    \item \textbf{Độ ổn định số học:} Quan sát hành vi của thuật toán khi đối mặt với các hệ phương trình có số điều kiện $\kappa(A)$ lớn (ill-conditioned).
    \item \textbf{Khả năng hội tụ:} Đánh giá tốc độ và điều kiện hội tụ của phương pháp lặp trong môi trường thực nghiệm.
\end{itemize}

\subsection{Chi tiết thiết kế và Cài đặt thuật toán}

\subsubsection{Chiến lược chọn phần tử chốt (Partial Pivoting)}
Để đảm bảo tính ổn định số học và tránh hiện tượng chia cho số gần bằng $0$, nhóm đã áp dụng kỹ thuật \textit{Partial Pivoting} trong Khử Gauss. Tại mỗi bước khử $k$, thuật toán thực hiện tìm hàng $p$ sao cho $|a_{pk}| = \max_{k \le i \le n} |a_{ik}|$, sau đó hoán đổi hàng $k$ và $p$. Điều này giúp giảm thiểu tối đa sai số làm tròn tích lũy trong quá trình khử.


\begin{lstlisting}[language=Python, style=pythonstyle, caption={Chọn pivot lớn nhất theo trị tuyệt đối}]
for i in range(r + 1, n):
    if abs(M[i][c_idx]) > abs(M[p][c_idx]):
        p = i
\end{lstlisting}



\subsubsection{Tối ưu hóa Phân rã LU}
Phương pháp LU được cài đặt theo dạng Doolittle (không pivot), tách rời chi phí phân rã $O(n^3)$ và chi phí giải hệ $O(n^2)$. Đây là kiến trúc tối ưu khi cần xử lý nhiều vector $b$ trên cùng một ma trận hệ số $A$.


\begin{lstlisting}[language=Python, style=pythonstyle, caption={Cập nhật ma trận L và U}]
U[i][k] = A[i][k] - sum_lu
L[k][i] = (A[k][i] - sum_lu) / U[i][i]
\end{lstlisting}

Tuy nhiên, do không sử dụng pivoting, phương pháp có thể mất ổn định khi các phần tử trên đường chéo của $U$ tiến gần về $0$.

\subsubsection{Cơ chế dừng của phương pháp lặp Gauss-Seidel}
Thuật toán lặp được thiết kế với điều kiện dừng dựa trên chuẩn Euclid:
\begin{equation}
    \|x^{(k)} - x^{(k-1)}\|_2 < 10^{-6}
\end{equation}



\begin{lstlisting}[language=Python, style=pythonstyle, caption={Cập nhật nghiệm Gauss-Seidel}]
x[i] = (b[i] - sigma) / A[i][i]
\end{lstlisting}

Ngoài ra, chương trình kiểm tra điều kiện chéo trội và đưa ra cảnh báo nếu không đảm bảo hội tụ.

\subsection{Kiểm chứng nghiệm (Solution Verification)}

Để đảm bảo tính đúng đắn của các thuật toán, nhóm tiến hành so sánh nghiệm thu được với thư viện chuẩn \texttt{NumPy}.


\begin{lstlisting}[language=Python, style=pythonstyle]
numpy.linalg.solve(A, b)
\end{lstlisting}

Kết quả cho thấy:
\begin{itemize}
    \item Với các ma trận điều kiện tốt (SPD, chéo trội), sai số tương đối đều nhỏ hơn $10^{-12}$.
    \item Các phương pháp trực tiếp (Gauss, LU) đạt độ chính xác tương đương NumPy.
    \item Gauss-Seidel có sai số lớn hơn ($10^{-6} \rightarrow 10^{-8}$) do bản chất lặp xấp xỉ.
\end{itemize}

\subsection{Cơ sở lý thuyết về tính ổn định và sai số}

\subsubsection{Số điều kiện (Condition Number)}
\begin{equation}
    \kappa(A) = \|A\| \cdot \|A^{-1}\|
\end{equation}

Sai số nghiệm bị chặn bởi:
\begin{equation}
    \frac{\|x - \hat{x}\|}{\|x\|} \le \kappa(A)\frac{\|\delta b\|}{\|b\|}
\end{equation}

Khi $\kappa(A)$ lớn, sai số đầu vào nhỏ có thể bị khuếch đại mạnh.

\subsubsection{Điều kiện hội tụ Gauss-Seidel}
\begin{equation}
    |a_{ii}| > \sum_{j \ne i} |a_{ij}|
\end{equation}

\subsection{Phân tích hiệu năng thời gian thực thi}

\subsubsection{Kịch bản thực nghiệm}
\begin{itemize}
    \item Ma trận chéo trội kích thước $n = 50 \rightarrow 1000$
    \item Nghiệm chuẩn $x = (1, \dots, 1)$
    \item $b = Ax$
    \item Thời gian trung bình sau 5 lần chạy
\end{itemize}

\subsubsection{Kết quả và Đánh giá độ phức tạp}

\begin{table}[h!]
\centering
\caption{Thời gian thực thi trung bình (giây)}
\begin{tabular}{lrrr}
\toprule
\textbf{$n$} & \textbf{Gauss} & \textbf{LU} & \textbf{Gauss-Seidel} \\
\midrule
50   & 0.0067 & 0.0048 & 0.0031 \\
100  & 0.0490 & 0.0339 & 0.0126 \\
200  & 0.3318 & 0.2782 & 0.0492 \\
500  & 5.1628 & 4.1701 & 0.3647 \\
1000 & 42.2038 & 47.1178 & 1.3831 \\
\bottomrule
\end{tabular}
\end{table}



\begin{figure}[h!]
    \centering
    % Đồ thị thời gian (Ảnh trên)
    \begin{subfigure}[b]{0.8\textwidth}
        \centering
        \includegraphics[width=0.8\textwidth]{image/time_plot.png}
        \caption{Đồ thị Log-Log so sánh thời gian thực thi (xác nhận độ phức tạp $O(n^3)$).}
        \label{fig:time_complexity}
    \end{subfigure}
    
    \vspace{0.8cm} % Khoảng cách giữa hai ảnh

    % Đồ thị sai số (Ảnh dưới)
    \begin{subfigure}[b]{0.8\textwidth}
        \centering
        \includegraphics[width=0.8\textwidth]{image/error_plot.png}
        \caption{Đánh giá sai số phần dư tương đối trên hệ ma trận điều kiện tốt.}
        \label{fig:relative_error}
    \end{subfigure}

    \caption{Phân tích hiệu năng và độ ổn định của các phương pháp giải hệ phương trình.}
    \label{fig:benchmark_results}
\end{figure}

Như thể hiện trong Hình 3 (bên dưới), các phương pháp Gauss và LU có độ dốc xấp xỉ $3$, phù hợp với $O(n^3)$. Điều này phản ánh việc các phương pháp trực tiếp thực hiện khử toàn bộ ma trận ($\sim n^3/3$ phép toán). Ngược lại, Gauss-Seidel chỉ thực hiện phép toán $O(n^2)$ mỗi vòng lặp, dẫn đến hiệu năng vượt trội khi số vòng lặp nhỏ.

\subsection{Đánh giá độ ổn định số học và sai số}

\subsubsection{Kết quả thực nghiệm}

\begin{table}[h!]
\centering
\caption{Sai số phần dư tương đối $\frac{\|Ax - b\|_2}{\|b\|_2}$}
\begin{tabular}{lcc}
\toprule
\textbf{Phương pháp} & \textbf{Ma trận SPD (Tốt)} & \textbf{Ma trận Hilbert (Xấu)} \\
\midrule
Khử Gauss (Pivoting) & $1.11 \times 10^{-16}$ & Không hội tụ/Tràn số \\
Phân rã LU           & $7.85 \times 10^{-17}$ & $1.03 \times 10^{-16}$ \\
Gauss-Seidel         & $4.85 \times 10^{-08}$ & $4.19 \times 10^{-06}$ \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Phân tích ma trận Hilbert}

Ma trận Hilbert là minh chứng điển hình cho sự giới hạn của tính toán số học đối với các hệ điều kiện kém:
\begin{itemize}
    \item \textbf{Mất ổn định (Numerical Instability):} Phương pháp LU cho sai số phần dư nhỏ, nhưng thực tế nghiệm $\hat{x}$ bị sai lệch rất lớn so với nghiệm gốc. Điều này do $\kappa(A)$ của ma trận Hilbert $10 \times 10$ lên tới xấp xỉ $1.6 \times 10^{13}$, khiến mọi sai số làm tròn $10^{-16}$ bị khuếch đại lên gần đơn vị.
    \item \textbf{Hạng số học (Numerical Rank):} Khi khử Gauss, thuật toán gặp phải các phần tử chốt cực nhỏ ($< 10^{-15}$). Trong tính toán thực tế, các giá trị này thường bị làm tròn về $0$, dẫn đến việc ma trận dù về lý thuyết là không suy biến nhưng về mặt số học lại bị coi là suy biến (singular), gây ra lỗi tràn số hoặc thất bại thuật toán.
    \item \textbf{Gauss-Seidel:} Hoàn toàn không hội tụ do vi phạm nghiêm trọng điều kiện chéo trội.
\end{itemize}

\subsection{Kết luận}

\begin{enumerate}
    \item \textbf{Partial Pivoting} là yếu tố bắt buộc để đảm bảo ổn định số học.
    \item \textbf{Phân rã LU} phù hợp khi giải nhiều hệ với cùng ma trận.
    \item \textbf{Gauss-Seidel} hiệu quả với ma trận chéo trội và kích thước lớn.
    \item Sai số phần dư nhỏ không đảm bảo nghiệm chính xác khi $\kappa(A)$ lớn.
\end{enumerate}

\subsection{Đánh giá tiêu chí hoàn thành}

Dựa trên thang điểm đánh giá của tài liệu hướng dẫn đồ án, nhóm đã rà soát và tự đánh giá mức độ hoàn thành các yêu cầu thực nghiệm và phân tích của Phần 3 như sau:

\begin{table}[H]
    \centering
    \renewcommand{\arraystretch}{1.4}
    \begin{tabular}{|p{4.5cm}|p{6cm}|c|}
        \hline
        \textbf{Tiêu chí} & \textbf{Mô tả yêu cầu} & \textbf{Mức độ hoàn thành} \\ \hline
        \textbf{Cài đặt phương pháp lặp} & Đúng, có kiểm tra điều kiện hội tụ & 100\% \\ \hline
        \textbf{Thực nghiệm thời gian} & Đủ kích thước, có đồ thị log-log & 100\% \\ \hline
        \textbf{Phân tích ổn định số} & Ma trận Hilbert vs ma trận ngẫu nhiên & 100\% \\ \hline
        \textbf{Nhận xét và kết luận} & Phân tích có chiều sâu, có số liệu & 100\% \\ \hline
        \textbf{Trình bày Notebook} & Rõ ràng, có visualization đầy đủ & 100\% \\ \hline
    \end{tabular}
    \caption{Đánh giá tiêu chí hoàn thành - Phần 3}
    \label{tab:criteria_part3}
\end{table}


% ==================== KẾT LUẬN ====================
\newpage
\section{Kết Luận}

\begin{softbox}
Sau một thời gian nghiên cứu và tiến hành cài đặt, Đồ án 1 với chủ đề \textbf{"Ma Trận và Cơ Sở của Tính Toán Khoa Học"} đã được nhóm hoàn thiện thành công. Quá trình thực hiện đồ án không chỉ giúp củng cố nền tảng lý thuyết mà còn mang lại những kinh nghiệm thực tiễn quý giá về khoa học thông qua việc lập trình trực tiếp các thuật toán mà không sử dụng các thư viện có sẵn.
\end{softbox}

\subsection{Tóm tắt kết quả đạt được}
Nhóm đã bám sát đề bài và hoàn thành xuất sắc các yêu cầu đề ra, cụ thể:
\begin{itemize}
    \item \textbf{Cài đặt thuật toán cốt lõi:} Triển khai hoàn toàn thủ công các thuật toán: Khử Gauss có chọn phần tử chốt (Partial Pivoting), các thuật toán phân rã như SVD bằng ngôn ngữ Python. Thuật toán đảm bảo độ chính xác nhờ cấu trúc dữ liệu \code{Fraction}, loại bỏ sai số làm tròn.
    \item \textbf{Giải quyết bài toán thực tế:} Áp dụng thành công phân rã SVD vào kỹ thuật xấp xỉ ma trận hạng thấp (Low-Rank Approximation) để tiến hành nén ảnh kỹ thuật số, hiểu hơn về tầm quan trọng của nén ảnh trong thực tế.
    \item \textbf{Trực quan hóa sinh động:} Xây dựng thành công chuỗi video học thuật bằng công cụ đồ họa \textbf{Manim}, biến các công thức đại số trừu tượng thành những đoạn phim trực tiếp giải thích cặn kẽ bản chất hình học của từng bước phân rã SVD và quá trình chéo hóa.
    \item \textbf{Phân tích độ ổn định số:} Đánh giá thực nghiệm sâu sắc về hiện tượng mất ổn định (Ill-conditioned) trên ma trận Hilbert và sự khác biệt về tốc độ hội tụ (Speed Benchmark) giữa các giải thuật.
\end{itemize}

\subsection{Khó khăn gặp phải}
Trong hành trình thực hiện đồ án, nhóm đã đối mặt và từng bước giải quyết một số rào cản nhất định:
\begin{itemize}
    \item \textbf{Tính toán khối lượng lớn:} Việc kiên quyết cài đặt bằng Python thuần (không dùng thư viện cấp C như NumPy) đã khiến thời gian thực thi thuật toán $O(n^3)$ trên các ma trận có kích thước khổng lồ ($n \ge 1000$) tăng cao, đòi hỏi phải tái cấu trúc vòng lặp nhiều lần để giảm chi phí hằng số.
    \item \textbf{Làm chủ công cụ đồ họa Manim:} Việc phải lập trình bằng code kịch bản đòi hỏi nhiều tính toán canh chỉnh tọa độ cho các đối tượng toán học để không bị vỡ bố cục, cùng với đó là việc đồng bộ nội dung, lên kịch bản và tốn tương đối nhiều thời gian để render video thành phẩm.
    \item \textbf{Xử lý ngoại lệ trong tính toán số:} Khó khăn trong việc bắt các lỗi suy biến hệ thống khi xử lý ma trận khuyết (Defective Matrix), cũng như mất mát chữ số phần thực (Loss of Significance) khi thử nghiệm giải hệ với ma trận cực kỳ xấu như ma trận Hilbert.
\end{itemize}

\subsection{Bài học rút ra}
Qua khoảng thời gian làm việc, những giá trị cốt lõi mà nhóm đã tích lũy được có thể rút kết thành:
\begin{itemize}
    \item \textbf{Giá trị của sự tối ưu phần cứng:} Hiểu rõ sự chênh lệnh to lớn về mặt hiệu năng khi chuyển đổi các vòng lặp cơ bản.
    \item \textbf{Tư duy toán học và lập trình hướng số liệu:} Nắm vững định nghĩa "Không gian vector" và các phép biến đổi điểm/trục cơ bản, từ đó mới có thể chuyển đổi thành công tư duy phân rã phương trình thành dạng hàm thuật toán có thể giải quyết bài toán lớn hơn của thực tiễn.
    \item \textbf{Kỹ năng làm việc nhóm:} Quá trình làm việc theo các thành phần chức năng (Solver, Demcomposition, Visualization) đã giúp các thành viên củng cố kỷ luật thiết kế phần mềm và rèn luyện kỹ năng giải quyết vấn đề rủi ro chung dưới áp lực của thời hạn yêu cầu.
\end{itemize}

% ==================== TÀI LIỆU THAM KHẢO ====================
\section{Tài liệu tham khảo}
\newpage
\begin{thebibliography}{99}
\bibitem{szeliski2022} 
Richard Szeliski. \textit{Computer Vision: Algorithms and Applications}, 2nd ed. Springer, 2022.

\bibitem{strang2023} 
Gilbert Strang. \textit{Introduction to Linear Algebra}, 6th ed. Wellesley-Cambridge Press, 2023.

\bibitem{golub2013} 
Gene H. Golub \& Charles F. Van Loan. \textit{Matrix Computations}, 4th ed. Johns Hopkins University Press, 2013.

\bibitem{manim} 
Manim Community Developers. \textit{Manim – Mathematical Animation Framework}, v0.18. 2024. \url{https://docs.manim.community}

\bibitem{3b1b} 
3Blue1Brown. \textit{Essence of Linear Algebra}. YouTube, 2016. \url{https://www.youtube.com/playlist?list=PLZHQObOWTQDPD3MizzM2xVFitgF8hE_ab}

\bibitem{Serrano.Academy}
Serrano.Academy. \textit{Singular Value Decomposition (SVD) and Image Compression}. Youtube, 2020. \url{https://www.youtube.com/watch?v=DG7YTlGnCEo&t=1117s}

\bibitem{Visual Kernel}
Visual Kernel. \textit{SVD Visualized, Singular Value Decomposition explained | SEE Matrix , Chapter 3}. Youtube, 2022. \url{https://www.youtube.com/watch?v=vSczTbgc8Rc}
\end{thebibliography}

\newpage
\section{Phụ lục}
\subsection{Mã nguồn}
Toàn bộ mã nguồn Python cài đặt các thuật toán (Khử Gauss, Phân rã SVD, Gauss-Seidel,...), kịch bản dựng hình Manim và các dữ liệu thực nghiệm benchmark của đồ án được quản lý phiên bản và lưu trữ công khai tại kho chứa GitHub. 

Thầy/cô có thể truy cập mã nguồn, cấu trúc thư mục (kèm file \code{requirements.txt} và \code{README.md}) tại đường dẫn sau:

\begin{itemize}
    \item \textbf{Kho lưu trữ mã nguồn (GitHub):} \url{https://github.com/ChickenDev-cyber/Project1_TUDTK}
\end{itemize}

\subsection{Video Manim}
Dưới đây là video trực quan hóa chéo hóa ma trận, phân rã và ứng dụng của SVD trong nén ảnh thực hiện bởi thư viện Manim. Thầy/cô có thể lựa chọn link Google Drive để xem trực tiếp hoặc tải về máy và sử dụng link Youtube để xem video rõ nét hơn
\begin{itemize}
    \item \textbf{Google Drive:} \url{https://drive.google.com/file/d/1_PeFI-HJdFTDXKHrGPsUPj5t1vmDvpcO/view?usp=sharing}
    \item \textbf{Youtube:} \url{https://youtu.be/zUNkAvs_XCI}
\end{itemize}

\label{page:lastpage}
\end{document}



